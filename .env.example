
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public"

# Optional: For AwesomeAPI if you need higher limits or specific endpoints that require a token.
# The current implementation for currency conversion (json/last/) does not require a token.
# AWESOMEAPI_TOKEN="YOUR_AWESOMEAPI_TOKEN_IF_NEEDED"
