
# This file is an example. Create a .env.local file with your actual secrets.
# DO NOT COMMIT .env.local to your repository.

# Prisma
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public"

# AwesomeAPI (Optional: Token not strictly needed for /last endpoint for currency)
# AWESOMEAPI_TOKEN="YOUR_AWESOMEAPI_TOKEN_IF_NEEDED"

# Encryption keys for AuditLog details (HEX encoded)
# Generate these securely. Example using Node.js:
# require('crypto').randomBytes(32).toString('hex') // For KEY
# require('crypto').randomBytes(16).toString('hex') // For IV
AUDIT_ENCRYPTION_KEY=""
AUDIT_ENCRYPTION_IV=""
